# ===============================
# = SECURITY AND MANAGEMENT - ACTUATOR CONFIG /actuator
# ===============================

# See application.yml for the security and management configuration

# default environment profile
# In production, you must use: java -jar -Dspring.profiles.active=production techcamp-0.0.1-SNAPSHOT.jar
spring.profiles.active=development

# security.basic.enabled=false
# security.user.name=admin
# security.user.password=admin

# management.security.enabled=false
# management.server.port=8090
# management.endpoints.web.expose=*
# management.endpoint.shutdown.enabled=true


# ===============================
# = DATA SOURCE
# ===============================

# See application.yml for the database connection

# spring.datasource.driverClassName=com.microsoft.sqlserver.jdbc.SQLServerDriver
# spring.datasource.url=jdbc:sqlserver://localhost:1433;database=TOYOTA_Tech_Camp
# spring.datasource.username=techcamp
# spring.datasource.password=techcamp
# spring.jpa.properties.hibernate.default_schema=techcamp

# ===============================
# = JPA / HIBERNATE
# ===============================
# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager).

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto=none

# Allows Hibernate to generate SQL optimized for a particular DBMS
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServer2012Dialect



spring.jpa.properties.hibernate.connection.pool_size=1

# Show or not log for each sql query
spring.jpa.show-sql=true


# ===============================
# = SPRING DATA REST
# ===============================

# By default, Spring Data REST hosts a root collection of links at /.
spring.data.rest.base-path=/api

spring.data.rest.default-page-size=10

# DATA REST (RepositoryRestProperties)
# spring.data.rest.base-path= # Base path to be used by Spring Data REST to expose repository resources.
# spring.data.rest.default-page-size= # Default size of pages.
# spring.data.rest.detection-strategy=default # Strategy to use to determine which repositories get exposed.
# spring.data.rest.enable-enum-translation= # Enable enum value translation via the Spring Data REST default resource bundle.
# spring.data.rest.limit-param-name= # Name of the URL query string parameter that indicates how many results to return at once.
# spring.data.rest.max-page-size= # Maximum size of pages.
# spring.data.rest.page-param-name= # Name of the URL query string parameter that indicates what page to return.
# spring.data.rest.return-body-on-create= # Return a response body after creating an entity.
# spring.data.rest.return-body-on-update= # Return a response body after updating an entity.
# spring.data.rest.sort-param-name= # Name of the URL query string parameter that indicates what direction to sort results.







